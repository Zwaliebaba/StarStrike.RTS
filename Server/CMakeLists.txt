# ── Server executable ─────────────────────────────────────────────────────────
# Currently a placeholder. Will become the server exe when Server/main.cpp is added.
# add_executable(Server main.cpp)
# target_link_libraries(Server PRIVATE NeuronServer GameLogic NeuronCore)
